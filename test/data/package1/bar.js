const existing = require("existing-dep/file/is/here");
const folder = require("./folder");
const foler = require("exists-in-dev-deps");
const nonExisting = require("non-existing-dep/deep/file");

console.log("bar");
